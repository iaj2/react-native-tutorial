# react-native-tutorial

## Running the Project Locally

First, make sure you have `Expo Go` downloaded on your mobile device.

Open a terminal and navigate to the the project's top-level directory (where you `app`, `assets` and other folders are).

Run the following command:

`npx expo start`

Scan the QR code that appears in the terminal with your mobile device.
